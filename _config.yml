# Site settings
title: Tania & David
email: bla@bla.com
url: https://taniamarries.me
description: "¡Nos casamos y queremos celebrar junto contigo!"
baseurl: ""

collections:
  stories:
    sort_by: order
  banners:

sass:
  style: compressed

defaults:
  - scope:
      path: "photos/"
    values:
      image: true
  - scope:
      type: banners
    values:
      layout: banner

# Build settings
markdown: kramdown
permalink: pretty

exclude:
  - _compress_images_cache.yml

theme: jekyll-theme-easy-wedding
# remote_theme: cnorick/jekyll-theme-easy-wedding
